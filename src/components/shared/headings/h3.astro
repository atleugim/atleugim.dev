---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"h3">;

const { class: className, ...rest } = Astro.props;
---

<h3 class:list={["text-lg font-bold sm:text-xl", className]} {...rest}>
  <slot />
</h3>
