---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"h2">;

const { class: className, ...rest } = Astro.props;
---

<h2 class:list={["text-xl font-semibold sm:text-2xl", className]} {...rest}>
  <slot />
</h2>
